/**
 * UI-independent simulation lifecycle abstractions and a background execution
 * engine whose listener callbacks are delivered on the Swing event thread.
 * Implementations place computation in {@link edu.cnu.mdi.sim.Simulation} and
 * expose controls through {@link edu.cnu.mdi.sim.SimulationEngine}.
 */
package edu.cnu.mdi.sim;
